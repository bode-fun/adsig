# Enable all linters except the deprecated and annoying ones
linters:
  enable-all: true
  disable:
    - nosnakecase
    - exhaustivestruct
    - varcheck
    - interfacer
    - structcheck
    - maligned
    - scopelint
    - ifshort
    - golint
    - deadcode
    - depguard
